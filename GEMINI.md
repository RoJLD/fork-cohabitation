# Gemini agent context for fork-cohabitation

This project uses **[AGENTS.md](AGENTS.md)** as the canonical context
file for AI agents. Gemini CLI reads `GEMINI.md` by convention, so this
file exists as a pointer.

## Read this first

[AGENTS.md](AGENTS.md) — project description, file layout, conventions,
do's and don'ts, commit identity rules, and spec-before-plan discipline.

## Gemini-specific overrides

(None by default. Add Gemini-specific notes here if behaviour diverges
from the canonical agent contract.)
