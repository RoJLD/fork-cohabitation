# fork-cohabitation

Generic tooling to make a fork cohabit with its upstream: bump dry-run, patch-drift
guard, and release watch — driven by a per-repo config plus a multi-repo registry.
Extracted from the gitnexus cohabitation mechanism (see
`../gitnexus/docs/superpowers/specs/2026-05-29-*`). CLI usage is documented later.
